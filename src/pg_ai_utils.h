#ifndef _PG_AI_UTILS_H_
#define _PG_AI_UTILS_H_

#include <ctype.h>
#include <stdio.h>

int escape_encode(const char* src, const size_t src_len, char *dst, const size_t max_dst_len);
#endif /* _PG_AI_UTILS_H_ */
